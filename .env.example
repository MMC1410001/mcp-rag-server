# Anthropic API key. Used for answer generation and for reading images via the
# vision API during ingestion. Create one at:
#   https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=UPDATE HERE
